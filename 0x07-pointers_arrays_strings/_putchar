#include "main.h"
#include <unistd.h>
/**
* _putchar - writes char c to stdout
* @c: Char to print
*
* Return: on correct 1.
* on error, -1 is returned
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
