#include <stdio.h>
#include "main.h"

/**
 * print_diagonal - print diagoal
 * @n: parameter
 * Returns: no value
 */

void print_diagonal(int n)
{
int len, space;
if (n > 0)
{
for (space = 0; space < len; space++)
{
putchar(' ');
}
Putchar('\\');

if (len == (n - 10))
{
continue;
}
putchar('\n');
}
}
